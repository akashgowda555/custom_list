import React from "react";
import { Strip } from "./Strip";
// this split is for later to decide which type of strip to render whether magazine or diff
export const Rail = () => {
    return (<Strip />)
}